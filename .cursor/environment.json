{
    "install": "cp -n config.example.yaml config.yaml 2>/dev/null || true; cp -n .env.example .env 2>/dev/null || true; cd api && pip install -e \".[dev]\"; cd ../frontend && npm ci",
    "start": "sudo dockerd > /tmp/dockerd.log 2>&1 & sleep 3; sudo chmod 666 /var/run/docker.sock 2>/dev/null || true",
    "terminals": [
      {
        "snapshot": "snapshot-20260609-64093354-21eb-45d0-9e9a-e4b1c9141aa7",
        "name": "stack",
        "command": "docker compose up",
        "description": "Cuebox full stack (frontend :3000, API :8000, Postgres :5433)"
      }
    ]
  }