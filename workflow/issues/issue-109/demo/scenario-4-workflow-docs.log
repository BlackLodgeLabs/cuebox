=== Scenario 4: WORKFLOW.md documentation ===
Date: 2026-07-12T08:38:58Z
Commit: 043c431

--- defer:execute-active table row ---
175:| `defer:execute-active` | Demo deferred — `active_skill=execute` while stage rank &lt; `execute-ready` (not at terminal execute stage; issue #109) |

--- execute finalize / active_skill clearing ---
72:- **`handoff_pending`:** fresh remote lock wins; stale locks (>15 minutes) treated as null; local explicit `null` clears
73:- **`loops`:** per-counter **max** of remote and local (counters only increment; preserves Action-recorded totals when agents commit stale loop values)
80:Before merging a cloud-agent side branch (`cursor/issue-NNN-pr-MMM-*-agent-*` or any `*-agent-*` branch) into the main issue branch, always run the merge helper on `workflow.state.json` so `stage`, `agents`, and `loops` do not regress from stale side-branch state.
92:Cleared on successful agent record, explicit deferral, or after **15 minutes** (stale lock). While fresh, duplicate spawns for the same issue/skill are blocked.
112:| `execute-ready` | Spawn demo **or** execute (`--reopen`) | Re-open inferred via `cursor-workflow-infer-reopen.sh`; execute agent must clear `active_skill` on finalize (issue #109); defense-in-depth allows demo at `execute-ready` even with stale `active_skill=execute` |
149:Successful spawns use `cursor-workflow-record-spawn-on-branch.sh` — one branch push records `agents.<skill>`, `active_agent_id`, `active_skill`, `handoff_pending: null`, optional `status_comment_id`, and `updated_at`.
158:2. **Admission gate** — skip if peer recorded `agents.<skill>`; defer on fresh peer `handoff_pending`; for **demo** target, skip when `stage=execute-in-progress` or stage rank &lt; `execute-ready`, defer when `active_skill=execute` **and** stage rank &lt; `execute-ready` (issue #91 / #109 — at `execute-ready`, stage rank wins over stale skill lock).
167:If `record-spawn-on-branch.sh` fails after a successful POST, branch `handoff_pending` remains until stale (15 minutes) or recovery re-records — subsequent spawns defer on `pending-lock`. Handoff recovery uses the same refetch + gate path (no local-only bypass).
175:| `defer:execute-active` | Demo deferred — `active_skill=execute` while stage rank &lt; `execute-ready` (not at terminal execute stage; issue #109) |
179:### Execute finalize (`active_skill` clearing, issue #109)
181:On successful execute completion (including pass-back resume), the execute agent must set `stage: execute-ready` with `active_skill: null` (and optionally `active_agent_id: null`) in `workflow.state.json` before push — mirroring planning's `plan-ready` finalize. If the agent forgets, defense-in-depth admission still allows demo spawn at `execute-ready` (stage rank ≥ `execute-ready` wins over stale `active_skill=execute`). During active execute (`execute-in-progress`), demo remains blocked.
